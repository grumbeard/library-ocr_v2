<header>
  <%= render 'shared/searchbar' %>
</header>

<div class="reader-container">
<iframe srcdoc='
    <html style="height: 100%;">
      <head>
        <body style="height: 100%; line-height: 24px;">
          <div class="reader-text-container" style="max-width: 80vw; height: 100%; padding: 16px 32px; margin: 0 auto; background-color: #555555; color: #EFEFEF;">
          <% @book.text.split("\n\n").each do |line| %>
            <p><%= line %></p>
          <% end %>
          </div>
        </body>
      </head>
    </html>'

    class="reader-text-viewer"
  ></iframe>
</div>

<div class="pdf-view-btn btn">
  <%= link_to book_path(@book) do %>
    <span class="material-icons">auto_stories</span>
  <% end %>
</div>
